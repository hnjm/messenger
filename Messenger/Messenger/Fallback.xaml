<Window x:Class="Messenger.Fallback"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        FontSize="14"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize" SnapsToDevicePixels="True">
    <Grid MaxWidth="960" MaxHeight="480">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="uiHeadText" Grid.Row="0" Margin="10" Foreground="#FF000000" FontSize="24"/>
        <TextBlock Grid.Row="1" Margin="10,0" Foreground="#FF808080">异常信息</TextBlock>
        <TextBox x:Name="uiContentText" Grid.Row="3" Margin="5,0" FontSize="12" Foreground="#FFA0A0A0" Background="Transparent" BorderThickness="0" IsReadOnly="True"/>
        <Button Grid.Row="4" Margin="10" Width="80" HorizontalAlignment="Right" Click="_Click">确定</Button>
    </Grid>
</Window>
